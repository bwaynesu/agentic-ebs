# Generate step cards for <taskId>

Approach `<approachId>` has been chosen (see <dataDir>/tasks/<taskId>/approaches.md).

Follow the rules in <dataDir>/prompts/steps-guide.md to produce <dataDir>/tasks/<taskId>/steps.md for the task "<taskId>".
